apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: spark-mongodb-client-cert
  namespace: default
spec:
  secretName: spark-mongodb-client-secret
  issuerRef:
    name: mongo-ca-issuer
    kind: ClusterIssuer
  commonName: spark-client
  usages:
    - client auth